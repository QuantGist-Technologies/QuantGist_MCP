name: Docker build & deploy

# Builds a linux/arm64 image (the Coolify server is ARM64), pushes it to GHCR,
# then triggers a Coolify redeploy that pulls the pre-built image. Mirrors the
# quantgist-web pipeline so Coolify never has to build on the slow ARM box.

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "pyproject.toml"
      - "Dockerfile"
      - "docker-compose.yml"
      - ".github/workflows/docker-build.yml"
  workflow_dispatch:

env:
  IMAGE: ghcr.io/quantgist-technologies/quantgist-mcp

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (linux/arm64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Trigger Coolify deploy
        # Skips cleanly until the Coolify app uuid (a repo variable) is set.
        if: vars.COOLIFY_MCP_DEPLOY_UUID != ''
        env:
          COOLIFY_API_TOKEN: ${{ secrets.COOLIFY_API_TOKEN }}
        run: |
          curl -fsSL -X POST \
            -H "Authorization: Bearer $COOLIFY_API_TOKEN" \
            "https://coolify.quantgist.com/api/v1/deploy?uuid=${{ vars.COOLIFY_MCP_DEPLOY_UUID }}"

      - name: Wait for health
        # MCP_HEALTH_URL should be https://api.quantgist.com/mcp. A bare GET there
        # returns 307 (Starlette mount adds the trailing slash) — that uniquely proves
        # OUR container answered. 404 = routed to the backend; 502/000 = container down.
        if: vars.COOLIFY_MCP_DEPLOY_UUID != '' && vars.MCP_HEALTH_URL != ''
        run: |
          for i in $(seq 1 18); do
            code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 10 "${{ vars.MCP_HEALTH_URL }}" || echo 000)
            echo "attempt $i: HTTP $code"
            case "$code" in
              200|307) echo "healthy"; exit 0 ;;
            esac
            sleep 10
          done
          echo "health check did not pass in time" >&2; exit 1
